'use strict';

var React = require('react');

var App = React.createClass({
    render: function() {
        return (
            <div>somthing</div>
        )
    }
});

React.render(<App />, document.getElementById('app'));

